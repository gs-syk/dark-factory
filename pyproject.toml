[project]
name = "dark-factory"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "George Stoien", email = "george.stoien@stryker.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
dark-factory = "dark_factory:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
